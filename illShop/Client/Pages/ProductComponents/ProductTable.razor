
@*<MudTable Items="@ProductDtoList" Hover="true" Breakpoint="Breakpoint.Sm" RightAlignSmall="true">*@
@*   <MudTable ServerData="@(new Func<TableState, Task<TableData<Product>>>(GetServerData))"
          Hover="true" Breakpoint="Breakpoint.Sm" RightAlignSmall="true" 
          @ref="_table" RowsPerPage="4">
    <HeaderContent>
        <MudTh>Image</MudTh>
        <MudTh>Name</MudTh>
        <MudTh>Price</MudTh>
    </HeaderContent>
    <RowTemplate>
        <MudTd DataLabel="Image">
            <MudAvatar Color="Color.Transparent" Size="Size.Large" Image="@context.ImageUrl" />
        </MudTd>
        <MudTd DataLabel="Name">@context.ProductName</MudTd>
        <MudTd DataLabel="Price">$@context.Price</MudTd>
    </RowTemplate>
</MudTable>*@
@*@if (ProductDtoList.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th scope="col"></th>
                <th scope="col">ProductName</th>
                <th scope="col">Price</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var data in ProductDtoList)
            {
                <tr>
                    <td>
                        <img src="@data.ImageUrl" alt="product image" style="width:100px;" />
                    </td>
                    <td class="align-middle">
                        @data.ProductName
                    </td>
                    <td class="align-middle">
                        @data.Price
                    </td>
                    <td class="align-middle">
                        <button type="button" class="btn btn-info" @onclick="() => RedirectToUpdate(data.ProductId)">Update</button>
                    </td>
                    <td class="align-middle">
                        <button type="button" class="btn btn-danger" @onclick="() => Delete(data.ProductId)">Delete</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <span>
        Loading products...
    </span>
}*@